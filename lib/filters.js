top_news = function(options) {
  return Feed_Entries.find({feed_id: '1'}, options);
}
transfer_news = function(options) {
  return Feed_Entries.find({feed_id: '2'}, options);
}
analysis_news = function(options) {
  return Feed_Entries.find({feed_id: '3'}, options);
}
arsenal_news = function(options) {
  return Feed_Entries.find({feed_id: '4'}, options);
}
chelsea_news = function(options) {
  return Feed_Entries.find({feed_id: '5'}, options);
}
liverpool_news = function(options) {
  return Feed_Entries.find({feed_id: '6'}, options);
}
city_news = function(options) {
  return Feed_Entries.find({feed_id: '7'}, options);
}
united_news = function(options) {
  return Feed_Entries.find({feed_id: '8'}, options);
}
athletico_news = function(options) {
  return Feed_Entries.find({feed_id: '9'}, options);
}
barcelona_news = function(options) {
  return Feed_Entries.find({feed_id: '10'}, options);
}
madrid_news = function(options) {
  return Feed_Entries.find({feed_id: '11'}, options);
}
acmilan_news = function(options) {
  return Feed_Entries.find({feed_id: '12'}, options);
}
asroma_news = function(options) {
  return Feed_Entries.find({feed_id: '13'}, options);
}
inter_news = function(options) {
  return Feed_Entries.find({feed_id: '14'}, options);
}
juventus_news = function(options) {
  return Feed_Entries.find({feed_id: '15'}, options);
}
bayern_news = function(options) {
  return Feed_Entries.find({feed_id: '16'}, options);
}
leverkusen_news = function(options) {
  return Feed_Entries.find({feed_id: '17'}, options);
}
dortmund_news = function(options) {
  return Feed_Entries.find({feed_id: '18'}, options);
}
wolfsburg_news = function(options) {
  return Feed_Entries.find({feed_id: '19'}, options);
}
ucl_news = function(options) {
  return Feed_Entries.find({feed_id: '20'}, options);
}
