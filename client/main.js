embedly('on', 'card.rendered', function(iframe){
  // iframe is the card iframe that we used to render the event.
   var card = $(iframe);
 
  // Grab the width and height.
 this.contents().find('.provider provider-gt-35').css('background-color','red');
});